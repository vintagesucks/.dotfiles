- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [git config --global init.defaultBranch main]
